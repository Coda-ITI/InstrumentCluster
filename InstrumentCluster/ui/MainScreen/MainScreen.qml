import QtQuick 2.15
import InstrumentCluster 1.0

Rectangle {
    id: mainScreen
    color: ThemeSettings.isLightMode ? "#f0f0f0" : "#000000"
    width: parent.width
    height: parent.height * 7 / 8
}
